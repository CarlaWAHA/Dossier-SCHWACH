{
  "name": "dossier-schwach",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "cp -r dist/* ../backend/wwwroot/",
    "preview": "vite preview --port $PORT",
    "start": "vite preview --port $PORT"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "shx": "^0.4.0",
    "tailwindcss": "^3.3.0",
    "vite": "^4.5.0"
  }
}
